'use strict';

/**
 * reservation-form-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reservation-form-submission.reservation-form-submission');
